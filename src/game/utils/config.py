class GameConfig:
    def __init__(self, screen, window, sprites, fps, clock):
        self.screen = screen
        self.window = window
        self.sprites = sprites
        self.fps = fps
        self.clock = clock